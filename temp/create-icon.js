console.log("Creating a simple, reliable 1024x1024 icon"); const fs = require("fs"); const { createCanvas } = require("canvas"); const canvas = createCanvas(1024, 1024); const ctx = canvas.getContext("2d"); ctx.fillStyle = "#4285F4"; ctx.fillRect(0, 0, 1024, 1024); ctx.fillStyle = "#FFFFFF"; ctx.font = "bold 500px Arial"; ctx.textAlign = "center"; ctx.textBaseline = "middle"; ctx.fillText("F", 512, 512); const buffer = canvas.toBuffer("image/png"); fs.writeFileSync("assets/images/icon.png", buffer);
